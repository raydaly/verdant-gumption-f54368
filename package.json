{
  "name": "greatuncle",
  "version": "86.0.17",
  "description": "Family stewardship and connection app",
  "main": "src/ui/app.js",
  "scripts": {
    "start": "npx serve .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "shipit": "git add . && git commit -m 'v86.0.17 - Dynamic link generation for Pass on the Gift' && git push"
  }
}