{
  "name": "greatuncle",
  "version": "88.0.2",
  "description": "Family stewardship and connection app",
  "main": "src/ui/app.js",
  "type": "module",
  "scripts": {
    "start": "npx serve .",
    "test": "node --test tests/*.test.js",
    "shipit": "npm test && git add . && git commit -m 'v88.0.2 - Milestone age fixes and calendar filter integration' && git push"
  }
}