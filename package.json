{
  "name": "greatuncle",
  "version": "87.0.0",
  "description": "Family stewardship and connection app",
  "main": "src/ui/app.js",
  "type": "module",
  "scripts": {
    "start": "npx serve .",
    "test": "node --test tests/*.test.js",
    "shipit": "npm test && git add . && git commit -m 'v87.0.0 - Intentional Home Page (Log/Later workflow, Target Dates, Interactive Logging)' && git push"
  }
}